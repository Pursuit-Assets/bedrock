/**
 * Angie's home page.
 *
 * Owner: Angie.
 * Build whatever you want here — this stub is intentionally minimal so
 * you have a blank canvas. The only contract is the default export
 * (named export `HomeAngie`) wired into slugs.ts at /home/angie.
 *
 * Suggestions for what might live on a home:
 *   - the tasks/opportunities/awards you own this week
 *   - shortcuts to the views you visit most
 *   - personal scratchpad / quick notes
 *   - anything from the existing pages, recomposed
 */

export function HomeAngie() {
  return (
    <div className="mx-auto max-w-[1100px] px-7 py-8">
      <h1 className="text-[22px] font-semibold text-ink">Angie's home</h1>
      <p className="mt-2 text-[13px] text-ink-3">
        Stub. Edit `src/pages/home/HomeAngie.tsx` on the
        `home/angie` branch.
      </p>
    </div>
  );
}
